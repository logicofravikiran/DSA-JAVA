package Java_Concepts.Streams;

import java.util.*;

public class GroupWordsByLength {
    public static void main(String[] args) {
        String[] arr = { "Ravi", "Anvi", "Vedant", "Ajinkya", "Rohit", "Yash" };
        Map<Integer, Integer> map = new HashMap<>();

        for (String string : arr) {

        }
    }
}
